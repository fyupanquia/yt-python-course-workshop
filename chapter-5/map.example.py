# print(list(map(lambda *a: a, range(3))))

#print(list(map(lambda *a: a, range(3), 'abc')))

#print(list(map(lambda *a: a, range(3), 'abc', range(4, 7))))

#print(list(map(lambda *a: a, range(2), 'abc')))

print(list(map(lambda *a: a, range(4), 'abc')))